# Copy this file to .env and fill in your key. The .env file is gitignored.

# Your OpenRouter key. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# When true, the router only considers free models, so a run costs nothing.
FREE_MODE=false

# The port the web trace panel listens on.
PANEL_PORT=8000
